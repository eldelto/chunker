# WriteableChunkedFile.
# ReadonlyChunkedFile.
# Refactor to protocols.
# Handle index out of range.
# Use :ok instead of {:ok, nil}.
# Use defdelegate where possible.
# Move implementations into new directory.
# Rename WriteableChunkedFile to WriteableFile.
# Rename ReadonlyChunkedFile to ReadonlyFile.
# Overhaul public API to use "Chunker.*".
# Add linter and hexdocs to deps.
# Use custom exceptions in error tuple.
# Remove all TODOs.
# Documentation.
# Typespecs.
# Remove Chunker.chunks/1 for Chunker.length/1?
# Remove Chunker.path/1 because it is too specific?
# Use custom exception in Chunker.DiscBased.ReadonlyFile.
# Chunker.commit/1 should return a new ChunkedFile.
# AlreadyClosedError instead of AlreadyCommittedError.
# Move ReadOnlyError into Chunker.Exceptions module.
# Use Chunker.ChunkedFile.insert/3, remove/2 & get/2?
# Add Chunker.replace_chunk/3.
# Add Chunker.prepend_chunk/2.
- Refactor to lazy streams.